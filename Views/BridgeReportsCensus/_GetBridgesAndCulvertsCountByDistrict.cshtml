@model IEnumerable<ERA_BMS.ViewModels.BridgeCulvertDistrictSectionSegmentViewModel>

<hr />
<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title panel-info">Bridges and Culverts Count By <b>District</b></h3>
    </div>

    <div class="panel-body">
        <table class="table table-striped table-condensed table-responsive table-bordered display compact hover" id="bridgescountbydistrict">
            <thead>
                <tr>
                    <th>District No</th>
                    <th>District Name</th>
                    <th>No. of Bridges</th>
                    <th>No. of Culverts</th>
                </tr>
            </thead>
            <tbody>
                @{  int totalBridges = 0;
                    int totalCulverts = 0; 
                 }
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Districts.DistrictNo</td>
                        <td>@item.Districts.DistrictName</td>
                        <td>@item.BridgeCount</td>
                        <td>@item.CulvertCount</td>
                    </tr>
                    totalBridges += item.BridgeCount;
                    totalCulverts += item.CulvertCount;
                }
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        <h3 class="panel-title">No. of Districts: <b>@Model.Count()</b> | Total Bridges: <b>@totalBridges</b> | Total Culverts: <b>@totalCulverts</b></h3>
    </div>
</div>

<script>
    $(document).ready(function () {
        $('#bridgescountbydistrict').dataTable({
            paging: false,
            pageLength: 15,
            bLengthChange: true,
            scrollY: 400,
            
            searching: true,
            select: true,
            dom: 'lBfrtip',
            //buttons: ['copy', 'excel', 'pdf', 'print']
            buttons: [
                { extend: 'copy', className: 'btn btn-sm btn-primary' },
                { extend: 'excel', className: 'btn btn-sm btn-primary', text: 'Export Excel' },
                { extend: 'pdf', className: 'btn btn-sm btn-primary', text: 'Export PDF' },
                { extend: 'print', className: 'btn btn-sm btn-primary' }
            ]
        }).columnFilter({
            "aoColumns": [
                { "type": "number-range" },
                { "type": "text" },
                { "type": "select" },
                { "type": "date-range" }
            ]
        });
    });
</script>


