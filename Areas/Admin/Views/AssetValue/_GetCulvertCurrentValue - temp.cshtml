@model IEnumerable<ERA_BMS.ViewModels.CulvertValueViewModel>

<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title panel-info">Culvert Current Value (District: <b>@ViewBag.District</b>, Section: <b>@ViewBag.Section</b>, Segment: <b>@ViewBag.Segment</b>)</h3>
    </div>

    <div class="panel-body">
        <table class="table table-striped table-condensed table-responsive display compact hover" id="tblCulvertCurrentValue">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Culvert.CulvertId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CurrentValue.ConstructionYear)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CurrentValue.RC)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CurrentValue.ServiceYear)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CurrentValue.CV)
                    </th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Culvert.CulvertId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CurrentValue.ConstructionYear)
                        </td>
                        <td>
                            @item.CurrentValue.RC.Value.ToString("#,0.00")
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CurrentValue.ServiceYear)
                        </td>
                        <td>
                            @item.CurrentValue.CV.Value.ToString("#,0.00")
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        <h3 class="panel-title">Total No. of Culverts: <strong>@Model.Count()</strong></h3>
    </div>
</div>

<script>
    $(document).ready(function () {
        alert("Hello");
        $('#tblCulvertCurrentValue').dataTable({
            paging: true,
            pageLength: 15,
            bLengthChange: true,
            scrollY: 400,
            searching: true,
            select: true,
            dom: 'lBfrtip',
            //buttons: ['copy', 'excel', 'pdf', 'print']
            buttons: [
                { extend: 'copy', className: 'btn btn-sm btn-primary' },
                { extend: 'excel', className: 'btn btn-sm btn-primary', text: 'Export Excel' },
                { extend: 'pdf', className: 'btn btn-sm btn-primary', text: 'Export PDF' },
                { extend: 'print', className: 'btn btn-sm btn-primary' }
            ]
        });
    });
</script>
