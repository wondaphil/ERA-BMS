@model IEnumerable<ERA_BMS.Models.DeckSlabType>

@{
    ViewBag.Title = "Deck Slab Types";
}

<h2>Deck Slab Types</h2>

<p>
    <a href="@Url.Action("New")" class="btn btn-primary"><span class="glyphicon glyphicon-plus"></span> Register New</a>
</p>
<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title panel-info">List of Deck Slab Types</h3>
    </div>

    <div class="panel-body">
        <table class="table table-striped table-condensed table-responsive display compact hover" id="deckslabtypes">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.DeckSlabTypeId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DeckSlabTypeName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Remark)
                    </th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.DeckSlabTypeId)
                        </td>
                        <td>
                            @Html.ActionLink(@Html.DisplayFor(modelItem => item.DeckSlabTypeName).ToString(), "Details", new { id = item.DeckSlabTypeId })
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Remark)
                        </td>
                        <td>
                            <!-- with Tooltips -->
                            @Html.ActionLink(" ", "Edit", new { id = item.DeckSlabTypeId }, new { @class = "glyphicon glyphicon-edit", @data_toggle = "tooltip", title = "Edit" })
                        </td>
                        <td>
                            @Html.ActionLink(" ", "Delete", new { id = item.DeckSlabTypeId }, new { @class = "glyphicon glyphicon-trash", @data_toggle = "tooltip", title = "Delete" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        <h3 class="panel-title">Total No. of Deck Slab Types: <b>@Model.Count()</b></h3>
    </div>
</div>

<script>
    $(document).ready(function () {
        

        //$('#deckslabtypes').DataTable({
        //    paging: true,
        //    pageLength: 15,
        //    bLengthChange: true,
        //    scrollY: 400, 
        //    
        //    searching: true,
        //    select: true,
        //    dom: 'lBfrtip',
        //    //buttons: ['copy', 'excel', 'pdf', 'print']
        //    buttons: [
        //        { extend: 'copy', className: 'btn btn-sm btn-primary' },
        //        { extend: 'excel', className: 'btn btn-sm btn-primary', text: 'Export Excel' },
        //        { extend: 'pdf', className: 'btn btn-sm btn-primary', text: 'Export PDF' },
        //        { extend: 'print', className: 'btn btn-sm btn-primary' }
        //    ]
        //});
    });
</script>
