@model IEnumerable<ERA_BMS.Models.PierType>

@{
    ViewBag.Title = "Pier Types";
}

<h2>Pier Types</h2>

<p>
    <a href="@Url.Action("New")" class="btn btn-primary"><span class="glyphicon glyphicon-plus"></span> Register New</a>
</p>
<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title panel-info">List of Pier Types</h3>
    </div>

    <div class="panel-body">
        <table class="table table-striped table-condensed table-responsive display compact hover" id="piertypes">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.PierTypeId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.PierTypeName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Remark)
                    </th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.PierTypeId)
                        </td>
                        <td>
                            @Html.ActionLink(@Html.DisplayFor(modelItem => item.PierTypeName).ToString(), "Details", new { id = item.PierTypeId })
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Remark)
                        </td>
                        <td>
                            <!-- with Tooltips -->
                            @Html.ActionLink(" ", "Edit", new { id = item.PierTypeId }, new { @class = "glyphicon glyphicon-edit", @data_toggle = "tooltip", title = "Edit" })
                        </td>
                        <td>
                            @Html.ActionLink(" ", "Delete", new { id = item.PierTypeId }, new { @class = "glyphicon glyphicon-trash", @data_toggle = "tooltip", title = "Delete" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        <h3 class="panel-title">Total No. of Pier Types: <b>@Model.Count()</b></h3>
    </div>
</div>

<script>
    $(document).ready(function () {
        

        //$('#piertypes').DataTable({
        //    paging: true,
        //    pageLength: 15,
        //    bLengthChange: true,
        //    scrollY: 400, 
        //    
        //    searching: true,
        //    select: true,
        //    dom: 'lBfrtip',
        //    //buttons: ['copy', 'excel', 'pdf', 'print']
        //    buttons: [
        //        { extend: 'copy', className: 'btn btn-sm btn-primary' },
        //        { extend: 'excel', className: 'btn btn-sm btn-primary', text: 'Export Excel' },
        //        { extend: 'pdf', className: 'btn btn-sm btn-primary', text: 'Export PDF' },
        //        { extend: 'print', className: 'btn btn-sm btn-primary' }
        //    ]
        //});
    });
</script>
