@model IEnumerable<ERA_BMS.Models.ExpansionJointType>

@{
    ViewBag.Title = "Expansion Joint Types";
}

<h2>Expansion Joint Types</h2>

<p>
    <a href="@Url.Action("New")" class="btn btn-primary"><span class="glyphicon glyphicon-plus"></span> Register New</a>
</p>
<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title panel-info">List of Expansion Joint Types</h3>
    </div>

    <div class="panel-body">
        <table class="table table-striped table-condensed table-responsive display compact hover" id="expansiojointtypes">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.ExpansionJointTypeId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ExpansionJointTypeName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Remark)
                    </th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.ExpansionJointTypeId)
                        </td>
                        <td>
                            @Html.ActionLink(@Html.DisplayFor(modelItem => item.ExpansionJointTypeName).ToString(), "Details", new { id = item.ExpansionJointTypeId })
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Remark)
                        </td>
                        <td>
                            <!-- with Tooltips -->
                            @Html.ActionLink(" ", "Edit", new { id = item.ExpansionJointTypeId }, new { @class = "glyphicon glyphicon-edit", @data_toggle = "tooltip", title = "Edit" })
                        </td>
                        <td>
                            @Html.ActionLink(" ", "Delete", new { id = item.ExpansionJointTypeId }, new { @class = "glyphicon glyphicon-trash", @data_toggle = "tooltip", title = "Delete" })
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        <h3 class="panel-title">Total No. of Expansion Joint Types: <b>@Model.Count()</b></h3>
    </div>
</div>

<script>
    $(document).ready(function () {
        

        //$('#expansiojointtypes').DataTable({
        //    paging: true,
        //    pageLength: 15,
        //    bLengthChange: true,
        //    scrollY: 400, 
        //    
        //    searching: true,
        //    select: true,
        //    dom: 'lBfrtip',
        //    //buttons: ['copy', 'excel', 'pdf', 'print']
        //    buttons: [
        //        { extend: 'copy', className: 'btn btn-sm btn-primary' },
        //        { extend: 'excel', className: 'btn btn-sm btn-primary', text: 'Export Excel' },
        //        { extend: 'pdf', className: 'btn btn-sm btn-primary', text: 'Export PDF' },
        //        { extend: 'print', className: 'btn btn-sm btn-primary' }
        //    ]
        //});
    });
</script>
